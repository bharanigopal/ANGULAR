var msg = "Welcome TO typeScript";
var firstChar = msg.charAt(0);
var restChar = msg.substring(1);
var newMsg = firstChar.toUpperCase() + restChar.toLowerCase();
console.log(msg);
console.log(newMsg);
